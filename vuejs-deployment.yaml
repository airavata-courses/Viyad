apiVersion: v1
kind: Service
metadata:
  name: vuejs
  labels:
    app: vuejs
spec:
  ports:
    - port: 8080
  selector:
    app: vuejs
    tier: frontend
  type: LoadBalancer
---
apiVersion: apps/v1 
kind: Deployment
metadata:
  name: vuejs
  labels:
    app: vuejs
spec:
  selector:
    matchLabels:
      app: vuejs
      tier: frontend
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: vuejs
        tier: frontend
    spec:
      containers:
      - image: saiprasadrane/dockerize-vuejs-app:latest
        imagePullPolicy: IfNotPresent
        name: vuejs
        env:
        ports:
        - containerPort: 8080
          name: vuejs
        readinessProbe:
          httpGet:
            path: /
            port: 8080